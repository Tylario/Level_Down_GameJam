// Initialize variables
collision_timer = 0;   // Timer for tracking collision duration
animation_speed = 0;   // Speed for transitioning sprite frames
sprite_frame = 1;      // Default frame (frame index starts from 0)
frame_decrement_timer = 0; // Timer for slower decrement when on the last frame
